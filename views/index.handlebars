<!DOCTYPE html>
<html>
<head>
    <title>Bienvenida</title>
</head>
<body>
    <h1>Hola, {{user.first_name}} {{user.last_name}}</h1>
    <p>Bienvenido(a) a nuestro sitio.</p>
    

    <p>{{user.first_name}} {{user.last_name}}</p>
    <p>{{user.first_name}}</p>
    <p>{{user.last_name}}</p>
</body>
</html>